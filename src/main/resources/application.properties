spring.application.name=aulamaxvenda
spring.profiles.active=test
#se o atributo spring.jpa.open-in-view for igual a falso , isso desabilita a possiblidade
#do jackson que está no fim do ciclo de vida chamar o jpa para trazer os pedidos associados para o cliente
#não permite que um componente do fim do clclo de vida na hora de renderizar o json que ele 
#chame novamente o banco de dados 
spring.jpa.open-in-view=true